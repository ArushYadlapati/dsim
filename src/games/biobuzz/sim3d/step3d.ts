import type { RobotCommand, World } from '../../../types';

/** the 3D BIOBUZZ tick — `docs/biobuzz/plan-3d.md` §3.1. Not implemented yet (Day 1 lane A). */
export function step3d(world: World, dt: number, commands: Map<number, RobotCommand>): void {
  void world;
  void dt;
  void commands;
  throw new Error('biobuzz step3d: not implemented yet (Day 1 lane A)');
}
